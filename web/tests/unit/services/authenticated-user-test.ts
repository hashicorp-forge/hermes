import { module, test } from "qunit";
import { setupTest } from "ember-qunit";

module("Unit | Service | session", function (hooks) {
  setupTest(hooks);

  test("it loads the user info", async function (assert) {});
  test("it fetches user subscriptions", async function (assert) {});
  test("the subscription methods work as expected", async function (assert) {});
});
