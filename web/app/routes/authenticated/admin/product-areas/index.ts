import Route from "@ember/routing/route";

export default class AuthenticatedAdminProductAreasRoute extends Route {
  /**
   * We don't need a model because product/areas
   * are fetched in the `authenticated` route.
   */
}
