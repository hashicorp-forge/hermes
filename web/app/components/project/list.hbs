<div class="x-container mt-2 mb-4">

  {{! <div class="relative mb-6">
    <div class="mb-3 flex items-center gap-2.5">
      <FlightIcon @name="pin" />
      <h3 class="hermes-h4">Pinned</h3>
    </div>
  </div> }}

  <div class="relative mb-6">
    <nav class="flex gap-6 mb-10 justify-center">

      <div class="flex items-center gap-2">
        <FlightIcon @name="zap" />
        <h3 class="text-body-200 font-regular text-color-foreground-strong">
          Active
        </h3>
      </div>
      <div class="flex items-center gap-2">
        <FlightIcon @name="check-circle" />
        <h3 class="text-body-200 font-regular text-color-foreground-strong">
          Completed
        </h3>
      </div>
      <div class="flex items-center gap-2">
        <FlightIcon @name="archive" />
        <h3 class="text-body-200 font-regular text-color-foreground-strong">
          Archived
        </h3>
      </div>
    </nav>
    {{#if @projects}}
      <ol
        {{!-- {{did-insert this.configureMasonry}} --}}
        class="relative grid grid-cols-3 gap-2"
      >
        {{#each @projects as |project|}}
          <Project::Tile @project={{project}} />
        {{/each}}
        {{#each @projects as |project|}}
          <Project::Tile @project={{project}} />
        {{/each}}
        {{#each @projects as |project|}}
          <Project::Tile @project={{project}} />
        {{/each}}
      </ol>
    {{else}}
      {{! TODO: No projects state }}
    {{/if}}
  </div>

  <div class="mb-6">
    <div class="mb-3 flex items-center gap-2">
      <FlightIcon @name="check-circle" />
      <h3
        class="text-body-200 font-semibold text-color-foreground-strong"
      >Completed</h3>
    </div>
    {{#if @projects}}
      <ol class="grid">
        {{#each (take 3 @projects) as |project|}}
          <Project::Tile @project={{project}} />
        {{/each}}
      </ol>
    {{/if}}
  </div>

  <div class="mb-6">
    <div class="mb-3 flex items-center gap-2">
      <FlightIcon @name="archive" />
      <h3
        class="text-body-200 font-semibold text-color-foreground-strong"
      >Archived</h3>
    </div>
  </div>
</div>
