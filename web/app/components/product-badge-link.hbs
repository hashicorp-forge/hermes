<LinkTo
  @route="authenticated.product-area"
  @model={{@productArea}}
  class="product-badge-link"
  ...attributes
>
  <Hds::Badge
    @icon={{or (get-product-id this.args.productArea) "folder"}}
    @text={{this.productAreaName}}
    title={{this.productAreaName}}
  />
</LinkTo>
