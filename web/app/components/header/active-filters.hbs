{{#if this.toolbar.filtersAreShown}}
  Active filters:
  {{#each this.toolbar.docTypeFilters as |filter|}}
    {{filter}}
  {{/each}}
  {{#each this.toolbar.productFilters as |filter|}}
    {{filter}}
  {{/each}}
  {{#each this.toolbar.statusFilters as |filter|}}
    {{filter}}
  {{/each}}
  {{#each this.toolbar.ownerFilters as |filter|}}
    {{filter}}
  {{/each}}
{{/if}}
